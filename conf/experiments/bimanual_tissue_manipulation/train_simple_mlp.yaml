# @package _global_
defaults:
  - train_defaults
  - /agent_config: simple_mlp_agent
  - process_batch_config: defaults
  - encoder_config: state_mlp
  - _self_

wandb:
  group: alpha-test-btm-simplemlp
